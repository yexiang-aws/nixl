# Rust Examples

This directory contains Rust examples demonstrating NIXL usage from Rust bindings.

Available example:
- `src/single_process_example.rs`: Single-process transfer example

Notes:
- Ensure NIXL and its dependencies are built/installed as required by the Rust bindings.
- See the repository root `README.md` (Rust Bindings section) for details.
